# Main dependencies for pip-based installation
ezdxf>=1.0.0
svgwrite
gunicorn
Flask==2.3.3
Werkzeug==2.3.7
numpy
matplotlib

# Try pythonocc-core with compatible Python version
# Use older version that supports Python 3.13
pythonocc-core
